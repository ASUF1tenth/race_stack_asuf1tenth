version: '3.3'
services:
  nuc:
    build:
      context: .
      dockerfile: .devops/.devcontainer/Dockerfile
      args:
        - USERNAME=${USER}
        - UID=${UID}
        - GID=${GID}
    image: nuc_forzaeth_racestack_ros2:jazzy
